// js practice notes
const x = 10;
let name = "practice";

function greet(n) {
    return `hello ${n}`;
}

console.log(greet(name));

// array practice
const nums = [1, 2, 3, 4, 5];
const doubled = nums.map(n => n * 2);
console.log(doubled);

// TODO: try async/await next
